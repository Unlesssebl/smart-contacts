# Спецификация Web Frontend

## Стек

| Слой | Технология |
|------|-----------|
| Фреймворк | **React 19** + **Vite 7** |
| Язык | **TypeScript** |
| Маршрутизация | **React Router v8** |
| UI-библиотека | **Radix UI / shadcn-style components** + **Tailwind CSS 4** |
| Анимации | **motion/react** (Framer Motion) |
| HTTP-клиент | **Axios** (cookie-сессия, CSRF, refresh и обработка 503) |
| Управление состоянием | **Zustand** (useAppStore) + **persist** |
| Сборка | Vite (`npm run dev` → порт 5173) |

---

## Дизайн-система

- **Шрифт:** Inter
- **Цветовые токены CSS/Tailwind:**
  - `colorPrimary` — основной цвет компании
  - `colorSuccess` — верифицирован
  - `colorWarning` — ожидает действий (Gatekeeper, заявки)
  - `colorError` — блокировка, ошибки
- **Стилистика:** Apple-style (минимум рамок, обилие воздуха), glassmorphism (размытие фона, мягкие тени, для карточек `borderRadius: 24px`), светлая тема.
- **Визуализация сотрудников:** Аватарки из инициалов с эффектом стекла (градиент + backdrop-blur) на цветном фоне, цвет привязан к имени. Контакты оформлены в легкие круглые плашки.
- **Кнопки:** Консистентный мягкий стиль ("soft style") по всему приложению. Использование полупрозрачного цветного фона с насыщенным текстом в тон фона (например, `bg-blue-50 text-blue-600`), небольших отступов, скругленных углов (`rounded-md`) и мягкой тени (`shadow-sm`).
---

## Управление состоянием

Для сохранения минимального состояния интерфейса при перезагрузке используется middleware **persist** в Zustand:
- **Storage:** `localStorage`
- **Сохраняемые данные:** `isAuthenticated`, `currentUser`, `adSyncUnavailable`.
- **Сессионные токены:** защищённые cookie; JavaScript не хранит access token.
- **Логика:** При инициализации приложения Zustand автоматически восстанавливает состояние из хранилища.

### Производительность и оптимизация рендеринга
Поскольку `useAppStore` является единым стором для всего приложения, вызов `useAppStore()` без селекторов заставляет компонент перерисовываться при любых изменениях в хранилище (даже если изменились неиспользуемые компонентом свойства).

Для оптимизации производительности необходимо:
1. Использовать селекторы для выбора только необходимых свойств.
2. При выборе нескольких свойств использовать хук `useShallow` для предотвращения лишних рендеров:
   ```typescript
   import { useShallow } from 'zustand/react/shallow';

   const { prop1, prop2 } = useAppStore(
     useShallow((state) => ({
       prop1: state.prop1,
       prop2: state.prop2,
     }))
   );
   ```

---

## Структура проекта

```
services/web_frontend/
├── src/
│   ├── api/
│   │   ├── client.ts           # Базовый axios instance + interceptors
│   │   ├── auth.ts
│   │   ├── users.ts
│   │   ├── changeRequests.ts
│   │   └── reports.ts
│   ├── app/
│   │   ├── AppRouter.tsx       # Маршруты и lazy loading страниц
│   │   └── AppLifecycle.tsx    # Глобальные эффекты приложения
│   ├── features/
│   │   ├── administration/     # Нормализация административной очереди
│   │   └── profile/            # Общая логика редактирования профиля
│   ├── store/
│   │   ├── useAppStore.ts      # Композиция Zustand slices
│   │   └── slices/
│   ├── pages/
│   │   ├── LoginPage.tsx
│   │   ├── ProfilePage.tsx
│   │   ├── DirectoryPage.tsx
│   │   └── AdminPage.tsx
│   ├── components/
│   │   ├── GatekeeperModal.tsx
│   │   ├── EmployeeCard.tsx
│   │   ├── ProfileModal.tsx
│   │   ├── ReportModal.tsx
│   │   ├── ConnectionLostOverlay.tsx
│   │   └── ProtectedRoute.tsx
│   ├── App.tsx                 # Корневая композиция providers и overlays
│   └── main.tsx
├── Dockerfile
├── package.json
└── vite.config.ts
```

---

## Страницы и роуты

### `/login` — Вход
- **SSO Проверка**: При монтировании страницы выполняется `GET /auth/sso`.
  - Если успех → токен в стор → редирект на `/profile`.
  - Если 401 → отображение формы входа.
- **Форма**: `username` (AD-логин) и `password`.
- Успех → токен в `authStore` → редирект на `/profile`.
- 401 → сообщение «Неверный логин или пароль».
- **Анимация**: Плавное появление формы через `motion.div` при неудачном SSO.

---

### `/profile` — Личный кабинет
**Доступ:** все авторизованные роли.

**Логика при монтировании:**
1. Прочитать `is_verified` и `grace_period_left` из `authStore` (уже получены при логине — доп. запрос не нужен).
2. `!is_verified && grace_period_left == 0` → `<GatekeeperModal hardBlock={true}>` (нельзя закрыть)
3. `!is_verified && grace_period_left > 0` → `<GatekeeperModal hardBlock={false}>`
4. Иначе → рендер страницы

**Содержимое:**
- Карточка профиля (данные из `GET /auth/me`)
- Таблица «Мои заявки»: поле, новое значение, статус, дата
- Кнопка «Изменить данные» → форма с полями `internal_phone` (корпоративный внутренний, маска `99-99`), `mobile_phone` (мобильный, маска `+7 (999) 999-99-99`), `office_location` (только они редактируемы пользователем; остальные поля — из AD, не доступны для правки) → `POST /profile/me/change-request`
- Форма предзаполнена текущими значениями; поля, синхронизированные из AD (`full_name`, `department`, `job_title`), отображаются как read-only с пояснением «Синхронизируется из Active Directory»

---

### `/` — Справочник сотрудников
**Доступ:** все авторизованные.

**UI:** Поисковая строка по центру с кнопкой раскрытия расширенных фильтров. В панели фильтров доступны: выбор организации, отдела, должности (через `FilterCombobox`), а также переключатели «С телефоном» и «С email». Поддерживается пагинация (18 сотрудников на странице). Результаты — карточки (`<EmployeeCard>`). При загрузке — Skeleton Loaders. Если результатов нет — Empty State с кнопкой «Сбросить фильтры».

**Поведение `<UserCard>`:**
- Поля: имя, должность, отдел, кабинет, телефон, добавочный.
- Если поле `null` — плейсхолдер «Не указан» серым цветом (поле не скрывается).
- Длинные имена — `text-overflow: ellipsis`.

**Детальный профиль:** клик на карточку → модальное окно со всеми контактами (кликабельные).
- Для пользователей с правами администратора или IT-оператора (`admin` / `it_operator`) в модальном окне отображается блок «Администрирование» с красивой иерархией пути пользователя в Active Directory и кнопкой копирования Raw DN (само значение `ad_dn` на стороне бэкенда маскируется/устанавливается в `null` для непривилегированных пользователей).


**Кнопка «Сообщить об ошибке»** (`POST /reports`):
- Визуально второстепенна: `type="text"` / `size="small"`, расположена внизу модалки.
- При нажатии — появляется `<TextArea>` для описания ошибки (макс. 500 символов) и кнопка «Отправить».
- После отправки — `<Alert type="success">` «Спасибо, мы разберёмся»; форма скрывается.

---

### `/admin` — Пульт ИТ-оператора
**Доступ:** `<ProtectedRoute roles={['it_operator']}>`

**Разделы (вкладки):**
1. **Заявки** — таблица `<ChangeRequestTable>` всех `change_requests`; фильтр по статусу; кнопки «Одобрить» / «Отклонить»
2. **Репорты** — таблица `<ReportsTable>`; кнопка «Обработано» → `PATCH /reports/{id}/process`
3. **OU Маппинг (`<OuMappingPanel>`)** — управление сопоставлением OU Active Directory и предприятий:
   - Добавление и удаление связей между подразделением AD и наименованием организации.
   - Цвет язычка организации на карточке сотрудника привязывается к цвету аватарки пользователя (`avatarColor`) с фиксированным белым текстом.
   - Выбор из авторитетного дерева OU AD или ручной ввод ненайденных OU.

---



## UI States

| Состояние | Поведение |
|-----------|-----------|
| Загрузка списка | Skeleton Loaders (не спиннер) |
| Пустой результат поиска | Empty State + кнопка «Сбросить фильтры» |
| Ошибка API | Error Boundary — «Сервис временно недоступен» |
| Потеря соединения (API Down) | Глобальный оверлей `<ConnectionLostOverlay>` с текстом «Соединение потеряно. Мы пытаемся восстановить связь...». Возникает при 502/504/Network Error от API. |
| Глобальная проверка сессии | Полноэкранный лоадер |
| Gatekeeper — мягкая блокировка | Баннер над страницей: «Пожалуйста, проверьте актуальность ваших данных. Осталось пропусков: N» с кнопкой «Проверить» |
| Gatekeeper — жёсткая блокировка | Страница недоступна; модалку нельзя закрыть; отображается заголовок «Подтвердите ваши данные» |
| AD Синхронизация недоступна (503) | Глобальный баннер: «Синхронизация с AD временно недоступна. Изменения будут сохранены позже». Кнопка «Отправить заявку» заблокирована. |

---

## API-клиент (`src/api/client.ts`)

```typescript
const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
});

// Добавляет CSRF-токен из cookie для изменяющих запросов
apiClient.interceptors.request.use((config) => {
  const csrfToken = getCookie('csrf_token');
  if (csrfToken) config.headers['X-CSRF-Token'] = csrfToken;
  return config;
});

// Обработка ответов (Refresh Token, 503 и потери связи)
apiClient.interceptors.response.use(
  (res) => {
    // При успешном ответе сбрасываем флаги недоступности
    if (store.isApiDown) store.setApiDown(false);
    return res;
  },
  async (error) => {
    // Self-healing: Обработка потери связи (Vite proxy 502/504 или Network Error)
    if (!error.response || error.response.status === 502 || error.response.status === 504) {
      if (!store.isApiDown) store.setApiDown(true); // Включает полноэкранный оверлей
      return Promise.reject(error);
    }

    // При 401 → попытка refresh → при неудаче → /login
    if (error.response?.status === 401) {
      // refresh logic...
    }

    // При 503 → блокировка отправки заявок (AD Sync Unavailable)
    if (error.response?.status === 503) {
      // 1. Установить глобальный флаг adSyncUnavailable в store
      // 2. Показать глобальный toast-баннер
    }

    return Promise.reject(error);
  }
);
```

---

## Компонент `<GatekeeperModal>`

**Поведение:**
- **hardBlock**: если `true`, кнопка «Пропустить» скрыта, модалку нельзя закрыть (Esc/клик по фону заблокированы).
- **Счётчик пропусков**:
  - `grace_period_left == 2` → Жёлтый (Warning).
  - `grace_period_left == 1` → Красный (Error), текст «Последний пропуск».
  - `grace_period_left == 0` → Жёсткая блокировка.
- **Подавление и сессионный пропуск**: Модалка автоматически не отображается на страницах профиля (`/profile/*`) и подавляется после выбора «Напомнить позже» до смены пользователя.

**Действия:**
- «Всё верно» → `POST /profile/me/acknowledge { action: "confirm" }`.
- «Изменить данные» → закрыть модалку, скролл к форме редактирования на `/profile`.
- «Пропустить» → `POST /profile/me/acknowledge { action: "skip" }`.

---

## Переменные окружения

```env
# По умолчанию используется относительный путь /api/v1 (запросы проксируются на бэкенд)
VITE_API_BASE_URL=/api/v1
```

---

## Запуск

```bash
cd services/web_frontend
npm install
npm run dev   # http://localhost:5173
npm run check # typecheck + lint + tests + production build
```

---

## Синхронизация статусов и системных событий (WebSocket)

### `usePresence` Hook
Фронтенд отслеживает статус активности пользователя и подписывается на системные обновления с помощью React-хука `usePresence()`, который вызывается глобально (например, в корневом `App.tsx` при успешной авторизации).
- **Подключение**: Хук инициализирует WebSocket-соединение с `/ws/presence`, получая одноразовый JWT-токен.
- **Push-события вместо Polling**: 
  - Приложение полностью отказалось от HTTP-поллинга в пользу мгновенных WebSocket событий (`admin_update`, `profile_updated`, `ldap_status_updated`).
  - При получении события хук автоматически вызывает соответствующие методы `Zustand` (например, `fetchAdminData` или `fetchMyPendingFields`), чтобы реактивно обновить интерфейс без перезагрузки страницы.
- **Анализ бездействия**:
  - Навешиваются глобальные обработчики событий `mousemove`, `keydown`, `click`, `scroll`.
  - При отсутствии активности более **5 минут**, хук отправляет на бэкенд событие `{ "action": "set_presence", "status": "away" }`.
  - Любая активность возвращает статус пользователя в `"online"`.
- **Хранение данных в Zustand**:
  - Принятые статусы сохраняются в `globalPresence` словаре в `useAppStore`.
  - При обновлении/фильтрации справочника (`fetchUsers`), статусы из `globalPresence` автоматически накладываются поверх списка пользователей с сервера.
- **Визуализация индикаторов**:
  - **В сети (`online`)**: Зеленый пульсирующий круг (`bg-emerald-500` + CSS keyframes анимация).
  - **Отошел (`away`)**: Статичный желтый круг (`bg-amber-400`).
  - **Не в сети (`offline`)**: Пустой круг с серой обводкой (`bg-white border-slate-300`).
```
